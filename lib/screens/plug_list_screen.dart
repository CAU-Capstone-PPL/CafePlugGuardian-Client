import 'package:flutter/material.dart';

class PlugListScreen extends StatelessWidget {
  const PlugListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("여기는 플러그 리스트 페이지 입니다."),
    );
  }
}
